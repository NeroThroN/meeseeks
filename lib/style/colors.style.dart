part of 'themes.style.dart';

/// Custom app color palette
abstract class _Colors {
  /// App's primary color
  static const Color primary = Color(0xFF6BDFFF);

  /// App's color on primary
  static const Color onPrimary = Colors.black;

  /// App's secondary color
  static const Color secondary = Color(0xFFFF6314);

  /// App's color on secondary
  static const Color onSecondary = Colors.black;

  /// App's error color
  static const Color error = Colors.red;

  /// App's color on error
  static const Color onError = Colors.white;
}
